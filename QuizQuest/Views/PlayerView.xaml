<UserControl x:Class="QuizQuest.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizQuest.Views"
             DataContext="{Binding PlayerViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="AliceBlue">
    
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Label x:Name="counter" Content="{Binding TestData}" Margin="10" FontSize="40"
               VerticalAlignment="Center" HorizontalAlignment="Right"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            
            <Label x:Name="questionText" Content="Question?" 
               Margin="5" FontSize="50"/>
            
            <Label x:Name="questionUnderText" Content="Question of Question" 
                    Margin="5" FontSize="20" HorizontalContentAlignment="Center"/>
            
        </StackPanel>

        <WrapPanel Grid.Row="2" Orientation="Horizontal"
                   VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <WrapPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="150"/>
                    <Setter Property="Height" Value="60"/>
                    <Setter Property="Margin" Value="5"/>
                </Style>

            </WrapPanel.Resources>
            
            <StackPanel>
                <Button x:Name="answerButton1" Content="Button 1" Margin="5"
                        Command="{Binding UpdateButtonCommand}"/>
                <Button x:Name="answerButton2" Content="Button 2" Margin="5"/>
            </StackPanel>
            <StackPanel>
                <Button x:Name="answerButton3" Content="Button 3" Margin="5"/>
                <Button x:Name="answerButton4" Content="Button 4" Margin="5"/>
            </StackPanel>

        </WrapPanel>

        <!--<Button Grid.Row="2"
        VerticalAlignment="Center"
        HorizontalAlignment="Center"
        Content="{Binding Path=ActivePack.Difficulty}"
        Margin="10"/>-->



    </Grid>
</UserControl>
